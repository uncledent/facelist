/**
 * Measurement units to be used in design system
 */
export const units = {
  margin: 6,
  padding: 6,
  size: 10,
};

/**
 * Text sizes to be used in design system
 */
export const textSizes = {
  header1: 18,
  regularText: 16,
};

/**
 * Colors to be used in design system
 */
export const colors = {
  border: '#D3D3D3',
  background: 'white',
};
